Object.defineProperty(exports, '__esModule', { value: true });

var fileSelector = require('file-selector');
var vue = require('vue');
var core = require('@uploadthing/dropzone/core');

function useDropzone(options) {
    const optionsRef = vue.ref({
        disabled: false,
        maxSize: Number.POSITIVE_INFINITY,
        minSize: 0,
        multiple: true,
        maxFiles: 0,
        ...options
    });
    vue.watch(()=>({
            ...options
        }), (value)=>{
        optionsRef.value = {
            ...optionsRef.value,
            ...value
        };
    });
    const acceptAttr = vue.computed(()=>core.acceptPropAsAcceptAttr(optionsRef.value.accept));
    const rootRef = vue.ref();
    const inputRef = vue.ref();
    const dragTargets = vue.ref([]);
    const state = vue.reactive(core.initialState);
    const onWindowFocus = ()=>{
        if (state.isFileDialogActive) {
            setTimeout(()=>{
                if (inputRef.value) {
                    const { files } = inputRef.value;
                    if (!files?.length) {
                        state.isFileDialogActive = false;
                    }
                }
            }, 300);
        }
    };
    const onDocumentDrop = (event)=>{
        if (rootRef.value?.contains(event.target)) {
            // If we intercepted an event for our instance, let it propagate down to the instance's onDrop handler
            return;
        }
        event.preventDefault();
        dragTargets.value = [];
    };
    const onDocumentDragOver = (e)=>{
        e.preventDefault();
    };
    vue.onMounted(()=>{
        window.addEventListener("focus", onWindowFocus, false);
        document.addEventListener("dragover", onDocumentDragOver, false);
        document.addEventListener("drop", onDocumentDrop, false);
    });
    vue.onUnmounted(()=>{
        window.removeEventListener("focus", onWindowFocus, false);
        document.removeEventListener("dragover", onDocumentDragOver, false);
        document.removeEventListener("drop", onDocumentDrop, false);
    });
    const onDragenter = (event)=>{
        event.preventDefault();
        dragTargets.value = [
            ...dragTargets.value,
            event.target
        ];
        if (core.isEventWithFiles(event)) {
            Promise.resolve(fileSelector.fromEvent(event)).then((files)=>{
                if (core.isPropagationStopped(event)) return;
                const fileCount = files.length;
                const isDragAccept = fileCount > 0 && core.allFilesAccepted({
                    files: files,
                    accept: acceptAttr.value,
                    minSize: optionsRef.value.minSize,
                    maxSize: optionsRef.value.maxSize,
                    multiple: optionsRef.value.multiple,
                    maxFiles: optionsRef.value.maxFiles
                });
                const isDragReject = fileCount > 0 && !isDragAccept;
                state.isDragAccept = isDragAccept;
                state.isDragReject = isDragReject;
                state.isDragActive = true;
            }).catch(core.noop);
        }
    };
    const onDragover = (event)=>{
        event.preventDefault();
        event.stopPropagation();
        const hasFiles = core.isEventWithFiles(event);
        if (hasFiles && event.dataTransfer) {
            try {
                event.dataTransfer.dropEffect = "copy";
            } catch (err) {
                core.noop();
            }
        }
        return false;
    };
    const onDragleave = (event)=>{
        event.preventDefault();
        event.stopPropagation();
        // Only deactivate once the dropzone and all children have been lef
        const targets = dragTargets.value.filter((target)=>rootRef.value?.contains(target));
        // Make sure to remove a target present multiple times only once
        // (Firefox may fire dragenter/dragleave multiple times on the same element)
        const targetIdx = targets.indexOf(event.target);
        if (targetIdx !== -1) {
            targets.splice(targetIdx, 1);
        }
        dragTargets.value = targets;
        if (targets.length > 0) {
            return;
        }
        state.isDragActive = false;
        state.isDragAccept = false;
        state.isDragReject = false;
    };
    const setFiles = (files)=>{
        const acceptedFiles = [];
        files.forEach((file)=>{
            const accepted = core.isFileAccepted(file, acceptAttr.value);
            const sizeMatch = core.isValidSize(file, optionsRef.value.minSize, optionsRef.value.maxSize);
            if (accepted && sizeMatch) {
                acceptedFiles.push(file);
            }
        });
        if (!core.isValidQuantity(acceptedFiles, optionsRef.value.multiple, optionsRef.value.maxFiles)) {
            acceptedFiles.splice(0);
        }
        state.acceptedFiles = acceptedFiles;
        optionsRef.value.onDrop?.(acceptedFiles);
    };
    const onDrop = (event)=>{
        event.preventDefault();
        dragTargets.value = [];
        if (core.isEventWithFiles(event)) {
            Promise.resolve(fileSelector.fromEvent(event)).then((files)=>{
                if (core.isPropagationStopped(event)) {
                    return;
                }
                setFiles(files);
            }).catch(core.noop);
        }
        Object.assign(state, core.initialState);
    };
    const openFileDialog = ()=>{
        if (inputRef.value) {
            state.isFileDialogActive = true;
            inputRef.value.value = "";
            inputRef.value.click();
        }
    };
    const onKeydown = (event)=>{
        // Ignore keyboard events bubbling up the DOM tree
        if (!rootRef.value?.isEqualNode(event.target)) {
            return;
        }
        if (core.isEnterOrSpace(event)) {
            event.preventDefault();
            openFileDialog();
        }
    };
    const onInputElementClick = (event)=>{
        event.stopPropagation();
    };
    const onFocus = ()=>state.isFocused = true;
    const onBlur = ()=>state.isFocused = false;
    const onClick = ()=>{
        // In IE11/Edge the file-browser dialog is blocking, therefore, use setTimeout()
        // to ensure React can handle state changes
        // See: https://github.com/react-dropzone/react-dropzone/issues/450
        core.isIeOrEdge() ? setTimeout(openFileDialog, 0) : openFileDialog();
    };
    const getRootProps = ()=>({
            ref: rootRef,
            role: "presentation",
            ...!optionsRef.value.disabled ? {
                tabindex: 0,
                onKeydown,
                onFocus,
                onBlur,
                onClick,
                onDragenter,
                onDragover,
                onDragleave,
                onDrop
            } : {}
        });
    const getInputProps = ()=>({
            ref: inputRef,
            type: "file",
            style: "display: none",
            accept: acceptAttr.value ?? "",
            multiple: optionsRef.value.multiple,
            tabindex: -1,
            ...!optionsRef.value.disabled ? {
                onChange: onDrop,
                onClick: onInputElementClick
            } : {}
        });
    return {
        ...vue.toRefs(state),
        getInputProps,
        getRootProps,
        rootRef,
        inputRef
    };
}

exports.useDropzone = useDropzone;
